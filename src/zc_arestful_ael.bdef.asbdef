projection implementation in class ZBP_C_ARESTFUL_AEL unique;
strict ( 2 );
use draft;
define behavior for ZC_ARESTFUL_AEL alias Flights
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}